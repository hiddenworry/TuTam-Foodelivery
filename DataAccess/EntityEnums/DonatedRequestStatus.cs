namespace DataAccess.EntityEnums
{
    public enum DonatedRequestStatus
    {
        PENDING,
        ACCEPTED,
        REJECTED,
        CANCELED,
        EXPIRED,
        PROCESSING,
        FINISHED
    }
}
